package pt.cofinpro.prayingmantis.absences;

/** The fixed absence types. Stored by name (decision #8); the DB check lists the same values. */
public enum AbsenceTypeCode {
    VACATION,
    SICK,
    PARENTAL,
    UNPAID,
    TRAINING
}
