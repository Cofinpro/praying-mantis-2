# Shell scripts run inside Linux containers and CI, where a CRLF checkout breaks them
# ("/bin/sh^M: not found"). Keep them LF even with core.autocrlf=true on Windows.
*.sh text eol=lf
backend/mvnw text eol=lf
