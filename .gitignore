# OS / editors
.DS_Store
.idea/
*.iml
.vscode/*
!.vscode/extensions.json

# Logs
*.log


# Secrets / local config
.env
.env.*
!.env.example
# Only the MSW toggle for `pnpm dev:mock`. VITE_ variables end up in the browser bundle, so never secrets
!frontend/.env.mock
.claude/settings.local.json
